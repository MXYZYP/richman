# 换行统一：仓库内一律用 LF。检出到 Windows 也不转成 CRLF，
# 避免服务器上的 .sh 脚本出现 ^M 导致 "bad interpreter" 报错。
* text=auto eol=lf

# Windows 批处理必须保持 CRLF（cmd.exe 需要）
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
