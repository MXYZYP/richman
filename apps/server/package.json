{
  "name": "@richman/server",
  "version": "0.1.0",
  "private": true,
  "description": "阶段 2 实现：Node + Socket.IO 服务器（房间管理 + JSON 存档 + 静态托管）",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@richman/board-data": "workspace:*",
    "@richman/engine": "workspace:*",
    "@richman/protocol": "workspace:*",
    "sirv": "^2.0.4",
    "socket.io": "^4.8.1",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "socket.io-client": "^4.8.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
